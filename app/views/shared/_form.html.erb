<%= simple_form_for(@article) do |f| %>
    <%= f.input :title %>
    <%= f.input :content  %>
    <%= f.submit "#{ @article.persisted? ? 'Update' : 'Create' } my article"  %>
<% end %>

<%= link_to "Back", articles_path %>
